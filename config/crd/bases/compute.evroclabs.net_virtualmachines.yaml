---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: virtualmachines.compute.evroclabs.net
spec:
  group: compute.evroclabs.net
  names:
    kind: VirtualMachine
    listKind: VirtualMachineList
    plural: virtualmachines
    singular: virtualmachine
  scope: Namespaced
  versions:
  - name: compute
    schema:
      openAPIV3Schema:
        description: VirtualMachine is the Schema for the virtualmachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualMachineSpec defines the desired state of VirtualMachine
            properties:
              diskRefs:
                items:
                  properties:
                    bootFrom:
                      type: boolean
                    name:
                      type: string
                  required:
                  - bootFrom
                  - name
                  type: object
                type: array
              networking:
                properties:
                  publicIPv4Address:
                    properties:
                      static:
                        properties:
                          publicIPRef:
                            type: string
                        required:
                        - publicIPRef
                        type: object
                    type: object
                  securityGroups:
                    properties:
                      securityGroupMemberships:
                        items:
                          properties:
                            name:
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                type: object
              osSettings:
                properties:
                  cloudInitUserData:
                    type: string
                  ssh:
                    properties:
                      authorizedKeys:
                        items:
                          properties:
                            value:
                              type: string
                          required:
                          - value
                          type: object
                        type: array
                    type: object
                type: object
              running:
                type: boolean
              vmVirtualResourcesRef:
                properties:
                  vmVirtualResourcesRefName:
                    type: string
                required:
                - vmVirtualResourcesRefName
                type: object
            required:
            - diskRefs
            - vmVirtualResourcesRef
            type: object
          status:
            description: VirtualMachineStatus defines the observed state of VirtualMachine
            properties:
              networking:
                description: The current status of the networking set up on the VM
                properties:
                  privateIPv4Address:
                    description: The assigned private IPv4 address of the VM
                    type: string
                  publicIPv4Address:
                    description: The assigned public IPv4 address of the VM
                    type: string
                type: object
              virtualMachineStatus:
                description: The status of the VM (e.g., "Running", "Stopped", "Creating")
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
